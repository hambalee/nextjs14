import { Metadata } from "next"

export const metadata: Metadata = {
    title: "About Code",
}

export default function About() {
    return (
        <div>
            <h1>About me</h1>
        </div>
    )
};
